---
micronaut:
  # templatize!
  metrics:
    enabled: true
    export:
      elastic:
        enabled: true
        step: PT1M
        host: "http://localhost:9200"
        # userName: username
        # password: password
  server:
    port: "${APPLICATION_SERVER_PORT:8088}"
  application:
    name: webrtcstats_gatekeeper
    authentication:
      hashAlgorithm: "${AUTHENTICATION_HASH_ALGORITHM:`SHA-512`}"
      stretching: "${AUTHENTICATION_STRETCHING:2}"
      saltSize: "${AUTHENTICATION_SALT_SIZE:32}"
#  security:
#    enabled: true
#    endpoints:
#      login:
#        enabled: true
#      oauth:
#        enabled: true
#    token:
#      jwt:
#        enabled: true
#        signatures:
#          secret:
#            generator:
#              secret: jwtsecretforgatekeeperrandomchars1234543465434565
---
kafka:
  bootstrap:
    servers: "${KAFKA_HOSTS:`localhost:9092`}"
---
builderConfigs:
  - "filename"
---
hikari:
  username:  "${HIKARI_USERNAME:root}"
  password: "${HIKARI_PASSWORD:password}"
  minIdleInMs: "${HIKARI_MIN_IDLE_IN_MS:100}"
  timeout: "${HIKARI_TIMEOUT_IN_MS:1000}"
  jdbcURL: "${HIKARI_JDBC:`jdbc:mysql://localhost:3306/ObserveRTC`}"
  jdbcDriver: "${JDBC_DRIVER:`com.mysql.cj.jdbc.Driver`}"
  maxPoolSize: "${HIKARI_MAX_POOL_SIZE:100}"
  dialect:  "${JOOQ_DIALECT:MySQL}"

  poolname: "${HIKARI_POOL_NAME:gatekeeper}"
---
#sink:
#  builder: ElasticsearchSink
#  configuration:

#datasources:
#  #  default:
#  #    driverClassName: ${JDBC_DRIVER:org.h2.Driver}
#  #    password: ${JDBC_PASSWORD:"password"}
#  #    url: ${JDBC_URL:`jdbc:h2:mem:test_db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MySQL`}
#  #    username: ${JDBC_USER:user}
#  default:
#    url: ${JDBC_URL:`jdbc:mysql://localhost:3306/wobserver`}
#    username: ${JDBC_USER:root}
#    password: ${JDBC_PASSWORD:password}
#    driverClassName: ${JDBC_DRIVER:com.mysql.cj.jdbc.Driver}
#    dialect: MYSQL
---
isc:
  streams:
    demo:
      samples: "${ISC_STREAMS_DEMO_SAMPLES_TOPIC:demoSamples}"

  