Helper script to generate schema related stuffs for the observer
---

## Usage

```shell
    npm i
    node index.js
```

Generate the protobuf java code: 
`protoc --java_out=./javaprotobuf ProtobufSamples.proto` 