apiVersion: v1
kind: Service
metadata:
  name: {{ include "webrtc-observer.fullname" . }}
  labels:
    {{- include "webrtc-observer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webrtc-observer.selectorLabels" . | nindent 4 }}
