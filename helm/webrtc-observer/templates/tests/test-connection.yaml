apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "webrtc-observer.fullname" . }}-test-connection"
  labels:
    {{- include "webrtc-observer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "webrtc-observer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
