---
jooq:
  dialect: MYSQL
---
hikari:
  jdbcURL: ${JDBC_URL:`jdbc:h2:~/WebRTCObserver;MODE=MYSQL;INIT=RUNSCRIPT FROM 'classpath:init-test.sql'`}
  username: ${JDBC_USER:user}
  password: ${JDBC_PASSWORD:password}
  jdbcDriver: ${JDBC_DRIVER:org.h2.Driver}
    
